import { Template } from 'meteor/templating';

Template.addGame.onCreated(function() {
  // Sayfa oluşturulduğunda çalışacak kodlar buraya eklenebilir
});

Template.addGame.helpers({
  // Şablonda kullanılacak yardımcı fonksiyonlar buraya eklenebilir
});
